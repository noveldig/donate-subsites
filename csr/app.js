fetch("data.json").then(r=>r.json()).then(d=>{const el=document.getElementById("list");if(!d.items.length){el.innerHTML="<p>暂无内容</p>";return;}el.innerHTML=d.items.map(it=>{const pct=d.goal>0?Math.min(100,Math.round(it.raised/d.goal*100)):0;const cur={usd:"$",cny:"¥",eur:"€",jpy:"¥"}[it.currency]||"";const fresh=it.latest?'<p class="fresh" style="font-size:12px;color:#8a8a8a">更新时间 '+it.latest+'</p>':"";return '<div class="card"><h3>'+it.title+'</h3><p>'+(it.summary||"")+'</p>'+'<div class="bar"><i style="width:'+pct+'%"></i></div>'+fresh+'<a class="btn" href="'+it.url+'" target="_blank">前往查看</a></div>';}).join("");}).catch(e=>{document.getElementById("list").innerHTML="加载失败";});